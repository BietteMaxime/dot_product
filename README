TODO:

LLVM IR

Clang can generate llvm bytecode that is then required to run with lli.
That allows JIT.

Compile with:
clang++ -std=c++14 -O3 -emit-llvm -msse4.1 -fno-use-cxa-atexit -c -o dot_product.bc dot_product.cpp

And then run with:
lli dot_product.bc

